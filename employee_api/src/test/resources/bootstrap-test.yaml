spring:
  application:
    name: employee-api
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: true
    loadbalancer:
      ribbon:
        enabled: false


