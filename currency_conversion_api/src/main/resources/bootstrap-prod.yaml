spring:
  application:
    name: currency-conversion-api
  cloud:
    kubernetes:
      enabled: true
      reload:
        enabled: true
      config:
        enabled: true
        name: currency-conversion-api
